To run the code
First extract the folder
Open the folder in terminal
Enter the following commands on ternal to compile the code :
      g++ boids.cpp -lGL -lGLU -lglut -lGLEW
After successfully compiling type the following to run the program
      ./a.out

Program should run successfully now.
INTERACTION for camera are printed on terminal when program is executed .
